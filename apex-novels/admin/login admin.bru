meta {
  name: login admin
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/account/admin/login
  body: json
  auth: inherit
}

body:json {
  {
    "email": "admin@system.com",
    "password": "Admin@123"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
